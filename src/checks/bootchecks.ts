import { type } from "os";
import chalk from "chalk";
const { bot } = require("../bot.js")
const { config } = require("../index.js")
export async function checks() {
    config.basic.owner-ids
  return null;
}

